# TinyAgent 项目完善与轻量功能迭代任务拆分 (TODO.md)

本文档从项目现有优化空间中，挑选出**实现复杂度适中、改动风险低、用户体验与技术完整度提升显著**的功能与体验优化项，整理为四大任务模块，分别分配给 **T**、**C**、**K**、**G** 四位成员协同推进。

---

## 一、 人员任务全景与负责领域

| 成员 | 负责模块 | 核心目标 |
|:---|:---|:---|
| **T** | **前端音画与阅读交互体验打磨** | 实现朗读音字同步高亮（指读功能）、阅读字号缩放与排版美化、故事导出为纯文本/排版打印 |
| **C** | **AI 故事创作流式体验与插图异步化** | 改造故事创作页接通真实 SSE 流式出字，插图异步加载防阻塞，提升出字流畅度 |
| **K** | **智能助手 UI 童趣化与意图徽章展示** | 改造 Assistant 工具调用轨迹的图文童趣徽章渲染、预设示例快捷气泡与空状态引导优化 |
| **G** | **后端数据一致性、多音色枚举与异常兜底** | 补全多角色音色元数据接口、分类 Emoji 统一收敛至后端配置、API 健壮性与健全文档注释 |

---

## 二、 文件编辑冲突与协同先后顺序规范

多位成员涉及编辑同一公共文件时，为避免代码覆盖冲突，必须严格遵循以下协作与先后执行顺序：

```text
公共文件协同先后顺序流：
1. 【第一阶段】G 先行统一底层枚举与接口：
   - 先编辑 backend/config.py, backend/serializers.py, backend/routers/generate.py
   - 确立统一的分类映射、多角色声音配置及 SSE 返回格式。
   
2. 【第二阶段】前端三位成员并行推进独立页面开发：
   - T 编辑 fronted/story-detail.html (详情与朗读)
   - C 编辑 fronted/story-generate.html (创作与流式)
   - K 编辑 fronted/assistant.html (助手与卡片)

3. 【第三阶段】公共样式与公共 JS 的收敛（若涉及共同文件）：
   - 公共样式 fronted/css/style.css:
     * 顺序：T 先追加阅读器与指读高亮样式；
     * 随后 C 追加流式生成状态样式；
     * 最后 K 追加智能助手图文徽章样式。
     * （原则：每个人均在 style.css 末尾以专属注释块独立追加，严禁修改既有基础类）
   - 公共脚本 fronted/js/api.js 与 fronted/js/common.js:
     * 优先由 C 补充流式辅助处理函数，T 和 K 复用即可。
```

---

## 三、 成员任务详细规划与编辑清单

### 1. T 任务包：前端音画与阅读交互打磨
- **任务目标**：提升故事详情页的阅读舒适度与听书沉浸感，实现适合低龄儿童的互动式读绘本体验。
- **具体需求**：
  1. **音字同步指读高亮**：完善 `SpeechSynthesisUtterance` 的 `onboundary` 事件监听，朗读进行时，当前正在被朗读的段落/句子添加平滑的高亮背景（`reading-highlight` 效果），暂停或结束时自动清理。
  2. **阅读舒适度工具栏**：在故事详情页标题下方增加辅助操作条，支持字号调节（小、中、大三种字号无级切换），方便家长与儿童护眼阅读。
  3. **故事导出/分享优化**：完善 `downloadStory()` 功能，生成排版整洁的 `.txt` 或轻量 Markdown 格式文本文件并触发浏览器下载；优化打印样式（`@media print`），隐去播放按钮只保留正文与配图。
- **需要编辑的文件**：
  - `fronted/story-detail.html`（核心逻辑实现）
  - `fronted/css/style.css`（追加指读高亮动画、字号调节类及打印样式）
- **依赖与先后顺序**：
  - 可在基础版本上独立开发；涉及 `style.css` 时请在文件末尾新增 `/* === T: Reading & Karaoke Styles === */` 独立分区追加。

---

### 2. C 任务包：AI 故事创作流式体验与插图异步化
- **任务目标**：消除创作故事时的漫长等待焦虑，改造成实时逐词吐出效果，并解耦插图加载。
- **具体需求**：
  1. **接通真实流式接口**：将 `story-generate.html` 的生成逻辑对接后端现有的 SSE 接口或利用 `ReadableStream` 改造，让正文实时逐字打印，取代原来整篇完成后前端假延时的打字机。
  2. **插图异步并行生成**：生成故事正文时若勾选了“生成配图”，正文流式输出完毕后立即展示，同时卡片配图区展示友好的骨架屏/占位动效，在后台异步请求配图后淡入呈现。
  3. **快捷预设创意标签**：在故事主题输入框下方增加快捷创意标签（如“勇敢小熊”、“太空探险”、“分享魔法棒”），点击后自动填充主题与分类。
- **需要编辑的文件**：
  - `fronted/story-generate.html`（页面交互与流式对接）
  - `fronted/js/api.js`（完善 `API.generate.stream` 或增补流式 fetch 帮助方法）
  - `fronted/css/style.css`（追加流式光标闪烁动效、创意标签 Chips 与骨架屏样式）
- **依赖与先后顺序**：
  - 在 G 完成后端 SSE 数据格式确认后（或基于现有接口规范）即可开始；
  - 涉及 `fronted/js/api.js` 的改动需注意不要覆盖已有鉴权逻辑。

---

### 3. K 任务包：智能助手 UI 童趣化与意图徽章展示
- **任务目标**：将硬核的 Agent 工具轨迹转化为适合儿童与亲子理解的直观交互卡片。
- **具体需求**：
  1. **童趣化工具徽章重构**：重构助手消息气泡中的 `tool_steps` 渲染逻辑。不再展示生硬的英文工具名（如 `retrieve_story_knowledge`），而是展示趣味拟人徽章（例如：“📖 正在故事百宝箱中寻找相关线索”、“✨ 正在挥动魔法棒构思新故事”），并提供可折叠的小抽屉展示具体参数与执行结果。
  2. **引用来源卡片直达**：点击气泡底部的引用故事来源（`references`）时，支持直接平滑弹窗预览故事梗概，或附带跳转至详情页的超链接。
  3. **初始空状态与引导语丰富**：在未开始会话的空状态下，增加 4~6 个涵盖“找故事”、“解疑难”、“写新篇”维度的引导气泡，提升新用户的互动探索意愿。
- **需要编辑的文件**：
  - `fronted/assistant.html`（重构气泡、徽章与引用渲染逻辑）
  - `fronted/css/style.css`（追加童趣拟人徽章样式、抽屉折叠展开动画）
- **依赖与先后顺序**：
  - 纯前端表现层优化，可与后端并行推进；
  - 涉及 `style.css` 请在末尾追加 `/* === K: Assistant Fun Badges === */` 分区。

---

### 4. G 任务包：后端数据一致性、多音色枚举与异常兜底
- **任务目标**：完善后端基础数据服务，收敛全系统的映射枚举，健全接口规范与健壮性。
- **具体需求**：
  1. **丰富音色列表接口（TTS Mock/多音色）**：扩展 `backend/serializers.py` 中的 `voices()` 函数，由单一的“标准旁白”扩充为“温柔姐姐”、“博学树爷爷”、“活泼小萌兔”等 3~4 种儿童喜欢的音色元数据（头像、音色 ID、适用场景说明），使前端播放器的音色选择切换真正有丰富的反馈。
  2. **全系统分类与 Emoji 映射归一化**：将分散在 `database.py`、`generator_v2.py` 与前端硬编码的分类及 Emoji 字典，统一收敛在 `backend/config.py` 的 `CATEGORIES` / `EMOJIS`，并通过 `GET /api/v1/categories` 完整输出，避免前后端分类显示脱节。
  3. **生成接口异常兜底与注释健壮性**：为 `backend/routers/generate.py` 的流式接口补全针对空主题或生成异常的错误 SSE 格式响应，避免前端流式解析挂死。
- **需要编辑的文件**：
  - `backend/config.py`（统一映射配置）
  - `backend/serializers.py`（扩充多音色描述字典）
  - `backend/routers/generate.py`（SSE 接口异常流式响应优化）
  - `backend/routers/system_router.py`（确保分类及元数据完整透传）
- **依赖与先后顺序**：
  - **最先执行（第 1 顺位）**：G 优先完成配置与接口规范的统一，输出稳固的后端数据结构，便于前端三位成员调用与联调。

---

## 四、 阶段验收与联调检查表 (Checklist)

- [x] **G 交付物验收**：访问 `/api/v1/categories` 能取得包含 Emoji 与完整字段的列表；访问 `/api/v1/stories/{id}` 可见多元声音信息（已完成）。
- [x] **T 交付物验收**：打开任一故事详情页，点击朗读能看到文本跟随变色高亮；切换字号生效；点击下载故事能正常保存文本（已完成）。
- [x] **C 交付物验收**：创作故事页面点击创作，正文立即开始逐词涌出；配图区域正常并行渲染，无卡死现象（已完成）。
- [x] **K 交付物验收**：智能助手中询问“小熊的故事”，工具调用以图文徽章（如书本、魔法棒）直观呈现，引用故事点击可直接跳转查看（已完成）。
- [x] **总体验收**：执行项目自动化测试与语法校验，无报错与命名冲突，Git 提交历史清晰明了（全部完成）。


